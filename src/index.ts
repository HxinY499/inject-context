import injectContext, { configure } from './inject-context';

export type {
  SelectorItem,
  Selector,
  ContextItem,
  Configuer,
} from './inject-context';

export { configure };
export default injectContext;
