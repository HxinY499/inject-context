import injectContext, { defineSelector } from './inject-context';

export type { ContextItem } from './inject-context';

export { defineSelector };
export default injectContext;

const a = 1;
const c = 1;
